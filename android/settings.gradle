pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id 'com.android.application' version '7.0.4' apply false // Example, check your version
        id 'org.jetbrains.kotlin.android' version '1.7.0' // Update to required Kotlin version
    }
}
rootProject.name = "AppLock"
include ':app'
